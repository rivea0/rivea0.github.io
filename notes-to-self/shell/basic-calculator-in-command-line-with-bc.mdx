---
title: Basic calculator in command line with `bc`
tags: [Shell]
description: Basic calculator in command line with `bc`
slug: basic-calculator-in-command-line-with-bc
date: 2025-02-01
---

For example, doing basic addition with `bc`:

```shell
echo "1 + 3" | bc -l
```

outputs:

```
4
```

*`-l` provides the standard math library.*
